import type { NextConfig } from "next";

const nextConfig: NextConfig = {
 // output: 'export', -- have to remove for api routes to work rip gh-pages
  // distDir: 'dist',
  // images: {
  //   unoptimized: true,
  // },
};

export default nextConfig;
